<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      权限说明：
      INTERNET            - WebView 加载网页必须
      ACCESS_NETWORK_STATE - 断网时给出友好提示
      USE_FINGERPRINT     - 指纹快速登录（AndroidBridge.authenticate）
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- 声明需要 WebView -->
    <uses-feature
        android:name="android.software.webview"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/Theme.AGIPM"
        android:usesCleartextTraffic="true"
        android:hardwareAccelerated="true"
        tools:targetApi="31">

        <!--
          MainActivity 关键配置：
          screenOrientation = sensor
              → 跟随传感器自由切换竖屏 / 横屏（转动手机即自动旋转）。
                注意：私密空间(vault.html)仍可通过 AndroidBridge.setOrientation
                临时指定 portrait / landscape，离开时恢复为 sensor（自由旋转）。
          configChanges
              → 屏幕旋转等变化时不重建 Activity，避免网页重新加载
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="sensor"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|smallestScreenSize|density|uiMode"
            android:theme="@style/Theme.AGIPM"
            android:keepScreenOn="false"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
